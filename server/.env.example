# Copy to .env and edit. docker-compose reads it automatically.
#
# Leave RACECAR_API_KEY blank for initial bring-up. Set it once both sides
# (server + dash settings) agree on a value. Without it the server accepts
# any X-API-Key header (intentional for first-time-on-bench testing).
RACECAR_API_KEY=
